$.ready(function(){ 
    var 

});

var parseDOM = function(DOM) {

    DOM.children().forEach(function(element) {
        if (element.tagName === DIV) { // TODO: probably need <b>, <p> and all other shits

            // TODO
        }

    });
}

var getContentLength = function(DOMString, wordLength) {
    if (DOMString !== '') {
        return wordLength;
    } else {





        getContentLength(DOMString, );
    }
}
